/** @jsx jsx */
import { jsx, css } from '@emotion/core'
import { Nav } from './Nav'
import CardInfoText from './CardInfoText'

export const CardBody = ({children}) => {

    return (
        <div>
          {children}
        </div>
    )
}
